# API keys - configura al menos una de las siguientes
OPENAI_API_KEY=your_openai_api_key_here
GROQ_API_KEY=your_groq_api_key_here

# Configuración del proveedor (openai, groq, auto)
# Si se establece en 'auto', se usará el primer proveedor para el que se encuentre una clave API
LLM_PROVIDER=auto
